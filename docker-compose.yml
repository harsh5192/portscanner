version: '3.8'

services:
  scanner:
    build: .
    container_name: netsec-scanner
    volumes:
      - ./data:/app/data
      - ./reports:/app/reports
      - ./logs:/app/logs
    environment:
      - SCANNER_ENV=production
      - DATABASE_URL=sqlite:///./data/scanner.db
